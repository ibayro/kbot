# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: "ibayro"
  tag: "1.0.4-60ccf95"
  arch: amd64
secret:
  name: "kbot"
  env: "6162898381:AAEZ44QUaeD2aQjJY6hIg2EZaR_MS2Ke-ec"
  key: "token"
securityContext:
  privileged: true
